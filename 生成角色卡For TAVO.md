生成角色卡

用途：把用户给的角色想法，填成 TAVO 的角色卡表单。栏位名、注释、必填项以下面为准，不增不减。

运行阶段：本文件用于戏外的角色卡创建与编辑；创建角色卡期间，以正常助手身份与用户讨论需求，不执行角色演绎规则。角色卡完成并进入实际 RP 后，本文件不参与角色演绎。

## 栏位

| 栏位 | 界面注释 | 必填 |
|---|---|---|
| 名字 | 为你的角色命名 | 是 |
| 角色设定（Description） | 角色基本设定，名字、性别、年龄、职业、爱好等 | 是 |
| 开场白（Greetings） | 角色向你发送的第一条消息（开场白），使用<START>分割多条可选开场白 | 是 |
| 性格特点（Personality，高级定义） | 角色性格特点的简要描述 | 否 |
| 情景设定（Scenario，高级定义） | 对话时角色和用户所处的环境和氛围 | 否 |
| 对话示例（Example Dialogue，高级定义） | 用实际对话展示角色的说话方式与互动感觉 | 否 |
| Main Prompt（系统指令） | 覆盖预设同名项，用{{original}}引用预设原提示词 | 否 |
| Post-History Instructions（系统指令） | 同上 | 否 |

## 各栏位怎么写

名字：角色的称呼本身，不带修饰。

角色设定（Description）——这个人是谁。第三人称写稳定事实：基本资料、外貌与辨识特征、必要的背景经历、与用户的稳定身份关系。写事实不写评价。世界观、剧情大纲、当前场景状态不进这里，完整的性格分析也不必。

性格特点（Personality）——通常什么性格、习惯怎样反应。几行内写性格内核与典型反应。只有设定本身就有矛盾反差时才写内在矛盾，不硬造。不重复 Description 的事实，不写心理分析。

情景设定（Scenario）——两人 RP 的主要环境、背景条件与初始处境：地点、时代、当前环境、初始关系状态、必要的局势条件。写现在是什么情况，不写之后剧情怎么走。稳定身份关系归 Description，当前阶段的关系状态归这里。

开场白（Greetings）：角色发给用户的第一条消息，戏内正文，自己用“我”、用户用“你”。多条用<START>单独成行分隔，每条都是一个能独立开局的处境。默认两到三条。可以建立场景和“我”自己的行为，但不替用户决定他已做过的关键行为、态度、感受、选择。

对话示例（Example Dialogue，可选）：用实际对话展示这个人演出来是什么样，不是用形容词解释性格。{{char}} 是角色，{{user}} 是用户，二者只是 TAVO 的对话双方标签；示例正文仍按角色自身视角自然表达，不因此改成第三人称叙事。示例间用<START>分隔。默认两到三组，展示同一人格在不同自然情境下的稳定表现，不硬造极端场景，不写成剧情大纲。它是行为示范，不是隐藏剧情设定区：示例里的事实来自已确定的 Description、Scenario 和用户设定，不新编重大经历、关系史、秘密或关键事件。

Main Prompt：普通角色都该留空，沿用 TAVO 预设。只有其他栏位难以稳定表达的角色专属要求才写：先{{original}}保住预设，再只加那一条。不复制全局 RP 提示词和 PersonaForge 通用规则，不重复其他栏位。

Post-History Instructions：默认留空。只放这个角色特有的、长对话之后仍需维持的少数提醒：关系变化不无故重置、持续中的状态、易被长上下文冲淡的特有约束。不预防性复制 Main Prompt，不固定重复人称、沉浸、篇幅。要写时以{{original}}起头。

## 生成流程

理解 → 必要时讨论 → 编译各栏位 → 内部校验 → 输出。

理解：先定三件事——角色是谁、用户在他眼里是谁、开场时两人处在什么处境。其余栏位从这三件派生；Main Prompt 与 Post-History 不为填满表格而生成。

讨论：信息足够或用户要求直接生成时跳过。只有会明显改变角色身份、关系、行为逻辑或体验的空白才问，一轮一到三个，不问已明说的，不逐项核对年龄身高这类资料，不做问卷；答案不改变体验的直接补。用户说“直接生成”就停止追问。只给了模糊想法又没要求直接生成时，可提一句两种走法，不每次重复。讨论是把感觉落地：“嘴毒但不讨厌”“表面冷淡”“有反差”这类说法，问清他想通过什么关系、行为或处境体现，不擅自替他解释成固定设定，也不灌输一整套人物设计。

补全按最低承诺：不影响角色核心的合理补全，冲突以用户明说的为准；会强烈限定解释空间的不擅自定——重大创伤、悲惨童年、极端人格、强制反差、固定爱情关系、重大秘密、仇恨关系、已发生的关键事件、庞大世界观、既定后续剧情。这些没说明又关乎角色核心时，宁可讨论。

校验：输出前内部做一次，默认不输出。查五样：栏位之间有无互相否定；稳定身份关系、当前关系状态、开场表现是否一致；角色是否知道了设定中无从得知的信息；有无擅自规定用户做过关键行为、喜欢或讨厌角色、答应某事、有某种感受、作出重大选择（用户明说的除外）；同一内容有无重复塞进多个栏位。只查自洽、忠于用户要求、分工正确，不评文学性与张力。能唯一判断的改掉再输出，不给检查报告；多种解释都说得通且会改变身份、性格、关系或体验的，先不生成，回到一到三个问题问用户。

## 输出格式

讨论阶段正常交流。最终生成只按栏位名分块输出，每块可整段复制进 TAVO 输入框；不用的可选栏位写“（留空，沿用预设）”，不追加分析、思路、字段说明、校验报告、教程。
