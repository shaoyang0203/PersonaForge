# Compatibility

本文件只声明**实际验证状态**。任何未在此列出的环境，默认视为未验证。

本项目是纯 Markdown 文本，不含代码、脚本、依赖与网络请求。因此"兼容"在本仓库里指两件事：

1. 该环境能否按需加载 `skills/<name>/SKILL.md`（读取 frontmatter 的 `name` / `description`）。
2. 该环境能否在场景之间保留并回写 `runtime/` 描述的状态文本。

## Tested

**尚待系统性验证。**

v1.0.0 发布时，本仓库没有在任何一个具体的模型、客户端或 Agent Skills 运行环境上完成端到端实跑验证。已通过的是内容层面的审查（方法论一致性、去重、无绝对化审美、无编造规则、Skill 目录自足），不是兼容性测试。

补齐这一节需要：在真实环境中挂载 `rp-core.md` + 至少两个 Skill，跑够长轮次，记录加载行为与输出退化情况。在此之前，本节为空是有意为之。

> 仓库里存在 `skills/*/agents/openai.yaml` **不等于** 已在 OpenAI 相关环境验证通过。该文件只是格式适配，任何环境要进入本节都需要实测记录。

## Conceptually Compatible

以下是**结构上适配、但尚未实测**的一类环境。列在这里表示设计目标包含它们，不表示已经验证通过。

- 支持 Agent Skills 目录规范（`SKILL.md` + `references/`，带 `name` / `description` frontmatter）的 agent 客户端。
- 能够在对话过程中动态注入或撤除上下文片段的角色扮演前端。
- 由用户或应用手工维护外部状态文本（粘贴、文件、模板变量皆可）的本地模型工作流。
- 只能携带一段固定 System Prompt 的环境：此路径下只使用 `rp-core.md` 常驻，Skill 正文按需整段追加，属于降级用法而非设计目标之外的能力。

> **Conceptually Compatible 不等于已经测试通过。** 在没有实测记录之前，不要把它当作可用的保证。

## Not Yet Verified

下列常见环境**没有被测试过**，本仓库不对它们作出任何支持声明：

- OpenAI 相关运行环境（ChatGPT、Codex 等）。仓库已为五个 Skill 提供 `agents/openai.yaml` 元数据文件，但没有在该类环境中实际加载与运行过。
- 各类支持 Skills / Plugins / 扩展机制的 CLI 与桌面 agent（具体产品与版本均未测试）。
- 主流角色扮演前端与其扩展、预设、脚本机制（未测试）。
- 各家在线大模型的长上下文实跑表现与中文输出质量（未测试）。
- 各类本地推理后端与量化模型在小上下文窗口下的退化行为（未测试）。
- 任何自动路由、自动安装、向量检索式按需加载方案（本仓库不提供，也未验证过等价性）。

若你在某个环境实测成功或失败，欢迎提交 issue 说明：客户端与版本、挂载方式、上下文预算、跑了多少轮、观察到哪几项 `references/quality-check.md` 里的失败模式。实测记录会写入上面的 Tested 一节，并在 `CHANGELOG.md` 中登记。

## 已知限制

- **没有挂载机制就不可能有自动按需加载。** 在不支持 Skill 发现的环境里，`description` 中的触发边界只是一段给人看的说明，需要人工决定何时注入哪个 Skill。
- **Runtime Memory 需要外部承载。** 本仓库只规定文本结构与维护规则，不提供存储、压缩或跨会话持久化的实现。
- **短上下文窗口下需取舍。** `rp-core.md` 约 1100 中文字；Skill 各约 900–1200 中文字；references 更大。上下文预算不足时，优先级是：rp-core > 当前最需要的一个 SKILL.md > references。
- **不保证输出风格。** 本框架约束人物行为的推导与状态一致性，不约束文风、篇幅、人称，这些属于使用者自己的角色扮演规则。
